#pragma once
#include <limits>
#include <buPlatformTypes.h>

namespace buEngineSDK {
  struct BU_UTILITY_EXPORT NumericLimits {
    static const uint32 MAX_UINT32;
    static const uint32 EPSIOLON;
  };
}